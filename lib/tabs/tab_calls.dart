import 'package:flutter/material.dart';

class CallsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Calls",style: TextStyle(fontSize: 20.0),),);
  }
}
